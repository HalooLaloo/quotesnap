'use client'

interface OnboardingWrapperProps {
  servicesCount: number
  userId: string
}

// Onboarding wizard disabled - was causing screen dimming issues
export function OnboardingWrapper({ servicesCount, userId }: OnboardingWrapperProps) {
  return null
}
